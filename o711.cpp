#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, w1, w2, h1, h2;
    cin >> n >> w1 >> w2 >> h1 >> h2;
    int a[n];
    vector<int> rise;
    int v1 = pow(w1, 2) * h1;
    int v2 = pow(w2, 2) * h2;
    for (int i = 0; i < n; i++) cin >> a[i];

    for (int i = 0; i < n; i++) {
        int now = 0;
        if (a[i] + now < v1) {
            
        }
    }
    
}